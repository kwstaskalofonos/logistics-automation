package kk.base.core.repository;

import kk.base.core.entity.WebUserRole;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WebUserRoleRepository extends JpaRepository<WebUserRole, Long> {
}
